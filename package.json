{
  "name": "food-delivery-system-functions",
  "version": "1.0.0",
  "description": "Google Cloud Functions for an advanced food delivery system",
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0", // Use a recent version
    "firebase-functions": "^5.0.0", // Use a recent version
    // You might need other libraries your modules depend on, e.g.
    // "@google-cloud/firestore": "^7.0.0", // if your modules use it directly and not via admin.firestore()
    // "axios": "^1.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "build": "echo 'Build complete'", // Keep this if Firebase expects it, or remove if not used.
    "serve": "firebase emulators:start --only functions", // For local testing
    "deploy": "firebase deploy --only functions" // For deployment
  },
  "engines": {
    "node": "20" // Or your preferred Firebase Functions supported runtime
  }
}
