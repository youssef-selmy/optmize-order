// 039-type-definitions.js
// Placeholder for TypeScript type definitions or JSDoc definitions if not using TypeScript.
// This file would typically contain interfaces or type aliases.
// For JavaScript, this might be JSDoc comments within files or a shared file for common types.
// Example:
/**
 * @typedef {Object} Driver
 * @property {string} id
 * @property {{latitude: number, longitude: number}} location
 * @property {boolean} active
 * @property {boolean} isActive
 * @property {object} lastSeenOnline - Firestore Timestamp object
 * @property {Array<Object>} orderRequestData
 * @property {Array<string>} preferredVendors
 */
module.exports = {}; // Usually an empty export if just holding definitions
